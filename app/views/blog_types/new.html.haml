.title1 新建分类
#new_blog_type
	= form_for @blog_type, :method => "post" do |f|
		= f.text_field :name, :class => "title_input_word"
		= f.text_area :desc, :class => "text_input_word"
		= f.submit "创建", :class => "confirm_button"
		